---
interface Props {
		date: Date;
		locale?: string;
}

import { formatContentDate } from "@/lib/article-metadata";

const { date, locale = "en" } = Astro.props;
---

<time datetime={date.toISOString()} class="text-gray-600 dark:text-gray-400">
	{formatContentDate(date, locale)}
</time>
